PAINEL PASTORAL V9.6 — CORREÇÃO DO FLUXO PÓS-LOGIN

Arquivos para colocar diretamente na RAIZ do GitHub (sem pastas).

Railway — variáveis:
DATABASE_URL=<connection string do Neon>
SECRET_KEY=<sua chave>
COOKIE_SECURE=1
INITIAL_ADMIN_EMAIL=admin@painelpastoral.local
INITIAL_ADMIN_PASSWORD=AdminPastoral2026!
INITIAL_PASTOR_PASSWORD=123456

O que a V9.6 corrige:
- sincroniza a senha do administrador mesmo se ele já existir no Neon;
- sincroniza a senha provisória dos pastores existentes;
- remove espaços/aspas acidentais das variáveis de autenticação;
- /health mostra diagnóstico sem revelar senhas;
- diferencia credencial inválida de erro do front-end.

Depois do deploy:
1. Abra /health no domínio Railway e confirme status=ok e admin_existe=true.
2. Admin: admin@painelpastoral.local / AdminPastoral2026!
3. Pastores: e-mail cadastrado / 123456.


V9.6 — correção aplicada:
- após autenticação do administrador, carrega um distrito válido;
- ativa explicitamente o modo Administrador Geral;
- renderiza os dados antes de ocultar o login;
- abre a página Visão Geral;
- restaura corretamente sessão administrativa em recarregamentos;
- mantém as credenciais atuais e os 7 arquivos na raiz.


CORREÇÃO V9.6:
- Corrigida falha JavaScript após autenticação causada pela tentativa de preencher um elemento inexistente (tabelaPessoas3).
- Renderização das tabelas agora valida a existência do elemento antes de atualizá-lo.
- Mantidos login do Admin Geral, Neon, Railway e os 7 arquivos na raiz.
